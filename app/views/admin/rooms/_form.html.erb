<%= form_for(@admin_room) do |f| %>
  <% if @admin_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_room.errors.count, "error") %> prohibited this admin_room from being saved:</h2>

      <ul>
      <% @admin_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.text_field :level_id %>
    <%= f.text_field :status %>
    <%= f.text_field :left_nextp %>
    <%= f.text_field :right_nextp %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
